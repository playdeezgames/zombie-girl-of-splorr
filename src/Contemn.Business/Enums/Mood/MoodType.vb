Public Module MoodType
    Public ReadOnly Info As String = NameOf(Info)
    Public ReadOnly Danger As String = NameOf(Danger)
    Public ReadOnly Warning As String = NameOf(Warning)
    Public ReadOnly Heading As String = NameOf(Heading)
End Module
